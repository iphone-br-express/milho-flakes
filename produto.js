const API_BASE_URL="https://milho-flakes.onrender.com"; const $=id=>document.getElementById(id); const money=v=>Number(v).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const id=new URLSearchParams(location.search).get('id');
async function api(path){const r=await fetch(API_BASE_URL+path);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`HTTP ${r.status}`);return d;}
(async()=>{try{const d=await api('/api/products');const p=(d.products||[]).find(x=>x.id===id);if(!p)throw new Error('Produto não encontrado.');const base=`assets/products/${p.id}`;$("detail").innerHTML=`<div class="gallery"><div class="main-photo"><span class="sale big">20% OFF</span><img id="mainImage" src="${base}-front.svg" alt="${p.name}"></div><div class="thumbs"><button data-view="front"><img src="${base}-front.svg" alt="Frente"></button><button data-view="back"><img src="${base}-back.svg" alt="Traseira"></button><button data-view="side"><img src="${base}-side.svg" alt="Lateral"></button></div></div><section class="product-detail"><span class="eyebrow">OFERTA iPHONE EXPRESS</span><h1>${p.name}</h1><p class="storage large">${p.storage}</p><div class="old">De ${money(p.referencePrice)}</div><div class="detail-price">${money(p.price)}</div><div class="included"><span>✓ Frete grátis</span><span>✓ Valor final no checkout</span><span>✓ Pagamento via Pix</span></div><p class="desc">Produto selecionado: <strong>${p.name} ${p.storage}</strong>. O valor final é definido no servidor e o gateway recebe o preço correspondente ao produto escolhido.</p><a class="buy large-buy" href="checkout.html?id=${encodeURIComponent(p.id)}">Continuar para dados de entrega →</a><div class="secure">🔒 O navegador envia apenas o código do produto. O valor não é aceito pelo cliente e é definido no servidor.</div></section>`; document.querySelectorAll('.thumbs button').forEach(b=>b.addEventListener('click',()=>$("mainImage").src=`${base}-${b.dataset.view}.svg`));}catch(e){$("detail").innerHTML=`<div class="empty">${e.message}</div>`;}})();
